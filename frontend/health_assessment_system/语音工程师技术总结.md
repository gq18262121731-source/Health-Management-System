# 语音工程师技术总结

> 养生之道智能健康管理系统 - 语音交互模块

---

## 一、模块概览

语音工程师部分负责 **ASR（语音识别）、TTS（语音合成）、语音交互** 的完整语音处理链路，实现适老化的语音交互体验。

### 代码文件清单

| 目录 | 文件 | 功能 | 代码行数 |
|------|------|------|----------|
| **后端 API** | `web_digital_human/voice_api.py` | 语音服务 REST API | 374 行 |
| **前端服务** | `src/services/voiceApi.ts` | 前端语音 API 封装 | 260 行 |
| **前端组件** | `src/components/ui/VoiceInputButton.tsx` | 语音输入按钮组件 | 90 行 |
| **ASR-LLM-TTS 库** | `ASR-LLM-TTS-master/` | 语音处理工具库 | ~50+ 文件 |

**总代码量**：约 **1,500+ 行** 核心代码 + 外部语音库

---

## 二、技术架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           语音交互架构图                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                         前端 (React)                             │   │
│   │  ┌─────────────────┐          ┌─────────────────────────────┐   │   │
│   │  │ VoiceInputButton │   →     │   voiceApi.ts (API 封装)    │   │   │
│   │  │ 录音 + 状态显示   │          │ textToSpeech / speechToText │   │   │
│   │  └─────────────────┘          └─────────────────────────────┘   │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                                   ↓ HTTP                                 │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                       后端 API (Flask)                           │   │
│   │                       voice_api.py                               │   │
│   │  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐       │   │
│   │  │ /api/voice/  │    │ /api/voice/  │    │ /api/voice/  │       │   │
│   │  │    asr       │    │    tts       │    │    chat      │       │   │
│   │  │  语音→文本    │    │  文本→语音   │    │  语音对话    │       │   │
│   │  └──────────────┘    └──────────────┘    └──────────────┘       │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                                   ↓                                      │
│   ┌─────────────────────────────────────────────────────────────────┐   │
│   │                      底层引擎                                     │   │
│   │  ┌──────────────────────┐    ┌──────────────────────────────┐   │   │
│   │  │  SenseVoice (ASR)    │    │     Edge-TTS (TTS)           │   │   │
│   │  │  FunASR 框架         │    │     微软云语音合成            │   │   │
│   │  │  支持中文方言         │    │     多种音色可选              │   │   │
│   │  └──────────────────────┘    └──────────────────────────────┘   │   │
│   └─────────────────────────────────────────────────────────────────┘   │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 三、核心模块详解

### 3.1 ASR 语音识别（SenseVoice）

**文件**: `voice_api.py`

#### 技术选型
| 方案 | 优点 | 备注 |
|------|------|------|
| **SenseVoice** ✅ | 阿里开源，中文效果好，支持方言 | 当前使用 |
| Web Speech API | 浏览器原生，无需后端 | 兼容性差 |
| Whisper | OpenAI 开源，多语言 | 资源占用大 |

#### 核心代码
```python
def get_asr_model():
    """获取语音识别模型（懒加载）"""
    global _asr_model
    if _asr_model is None:
        from funasr import AutoModel
        model_dir = "iic/SenseVoiceSmall"
        _asr_model = AutoModel(model=model_dir, trust_remote_code=True)
    return _asr_model

def speech_to_text(audio_file: str) -> str:
    """语音转文本"""
    model = get_asr_model()
    result = model.generate(
        input=audio_file,
        cache={},
        language="auto",  # 自动检测语言
        use_itn=False,    # 不使用逆文本正则化
    )
    return result[0]['text'].split(">")[-1].strip()
```

#### API 接口
```
POST /api/voice/asr
Content-Type: multipart/form-data

Request:
  audio: <音频文件>  或  audio_base64: <base64编码>

Response:
{
  "success": true,
  "data": { "text": "识别出的文本" }
}
```

---

### 3.2 TTS 语音合成（Edge-TTS）

**文件**: `voice_api.py`

#### 技术选型
| 方案 | 优点 | 备注 |
|------|------|------|
| **Edge-TTS** ✅ | 免费、音质好、多音色 | 当前使用 |
| CosyVoice | 阿里开源，可克隆 | 资源占用大 |
| pyttsx3 | 离线、轻量 | 音质一般 |

#### 可用音色
```python
TTS_VOICES = {
    "zh-CN-XiaoyiNeural": "晓伊 (女声-温柔)",   # 默认
    "zh-CN-YunxiNeural":  "云希 (男声-阳光)",
    "zh-CN-XiaoxiaoNeural": "晓晓 (女声-活泼)",
    "zh-CN-YunyangNeural": "云扬 (男声-新闻)",
    "zh-CN-XiaochenNeural": "晓辰 (女声-客服)",
}
```

#### 核心代码
```python
async def text_to_speech_async(text: str, voice: str, output_file: str):
    """异步文本转语音"""
    import edge_tts
    communicate = edge_tts.Communicate(text, voice)
    await communicate.save(output_file)
    return True

def text_to_speech(text: str, voice: str = DEFAULT_VOICE) -> str:
    """文本转语音，返回音频文件路径"""
    timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
    output_file = os.path.join(OUTPUT_DIR, f"tts_{timestamp}.mp3")
    asyncio.run(text_to_speech_async(text, voice, output_file))
    return output_file
```

#### API 接口
```
POST /api/voice/tts
Content-Type: application/json

Request:
{
  "text": "你好，我是健康助手",
  "voice": "zh-CN-XiaoyiNeural",
  "include_base64": false
}

Response:
{
  "success": true,
  "data": {
    "audio_url": "/api/voice/audio/tts_20241203_123456.mp3",
    "filename": "tts_20241203_123456.mp3"
  }
}
```

---

### 3.3 前端语音服务

**文件**: `src/services/voiceApi.ts`

#### TypeScript 接口定义
```typescript
// TTS 响应
interface TTSResponse {
  success: boolean;
  data?: {
    audio_url: string;
    filename: string;
    audio_base64?: string;
  };
  error?: string;
}

// ASR 响应
interface ASRResponse {
  success: boolean;
  data?: { text: string };
  error?: string;
}
```

#### 核心函数
```typescript
// 文本转语音
export async function textToSpeech(
  text: string, 
  voice?: string
): Promise<TTSResponse>

// 语音转文本
export async function speechToText(
  audioBlob: Blob
): Promise<ASRResponse>

// 语音对话（ASR + LLM + TTS 一体化）
export async function voiceChat(
  audioBlob: Blob
): Promise<VoiceChatResponse>
```

---

### 3.4 前端语音按钮组件

**文件**: `src/components/ui/VoiceInputButton.tsx`

#### 组件特性
- 🎤 大按钮设计（适老化）
- 🔴 录音状态动画
- 📢 语音提示反馈
- 🎯 支持多种尺寸 (sm/md/lg)

#### 使用示例
```tsx
<VoiceInputButton 
  onVoiceResult={(text) => console.log('识别结果:', text)}
  size="lg"
/>
```

---

## 四、ASR-LLM-TTS 工具库

**目录**: `src/ASR-LLM-TTS-master/`

### 核心脚本

| 文件 | 功能 |
|------|------|
| `12_SenceVoice_QWen2.5_edgeTTS.py` | SenseVoice + Qwen + Edge-TTS 完整流程 |
| `13_SenceVoice_QWen2.5_edgeTTS_realTime.py` | 实时流式语音对话 |
| `6_Inference_funasr.py` | FunASR 推理示例 |
| `3_Inference_edgeTTS.py` | Edge-TTS 推理示例 |
| `1_Inference_CosyVoice.py` | CosyVoice 语音克隆示例 |

### 支持的模型

| 类型 | 模型 | 说明 |
|------|------|------|
| ASR | SenseVoice (Small/Large) | 阿里开源语音识别 |
| ASR | FunASR | 阿里语音识别框架 |
| LLM | Qwen2.5 | 通义千问大模型 |
| TTS | Edge-TTS | 微软云语音合成 |
| TTS | CosyVoice | 阿里开源语音克隆 |
| TTS | pyttsx3 | 离线语音合成 |

---

## 五、依赖库

```txt
# ASR
funasr>=1.0.0            # 阿里 FunASR 框架
modelscope               # 模型下载

# TTS  
edge-tts>=6.0.0          # 微软 Edge TTS
pyttsx3                  # 离线 TTS（可选）

# 音频处理
sounddevice              # 录音
scipy                    # 音频文件处理
pygame                   # 音频播放
numpy                    # 数值计算

# Web 框架
flask                    # REST API
```

---

## 六、适老化设计要点

### 6.1 语音交互特点

| 特性 | 实现 |
|------|------|
| **大按钮** | 录音按钮 56px × 56px（lg 尺寸 64px） |
| **状态反馈** | 录音时红色闪烁动画 |
| **语音提示** | 开始录音时播报"请开始说话" |
| **结果确认** | 识别完成后播报"已识别：xxx" |
| **温柔音色** | 默认使用"晓伊"温柔女声 |
| **慢语速** | TTS 语速适当放慢 |

### 6.2 错误处理

```typescript
// 浏览器不支持
if (!('webkitSpeechRecognition' in window)) {
  alert('您的浏览器不支持语音识别功能');
  return;
}

// 录音出错
recognition.onerror = () => {
  setIsListening(false);
  alert('语音识别出错，请重试');
};
```

---

## 七、API 接口汇总

| 接口 | 方法 | 功能 |
|------|------|------|
| `/api/voice/asr` | POST | 语音转文本 |
| `/api/voice/tts` | POST | 文本转语音 |
| `/api/voice/chat` | POST | 语音对话（ASR→LLM→TTS） |
| `/api/voice/voices` | GET | 获取可用音色列表 |
| `/api/voice/audio/<filename>` | GET | 获取音频文件 |

---

## 八、总结

### 技术亮点

1. **双引擎架构**：ASR (SenseVoice) + TTS (Edge-TTS) 组合
2. **适老化设计**：大按钮、语音反馈、温柔音色
3. **懒加载优化**：模型按需加载，减少启动时间
4. **异步处理**：TTS 使用 asyncio 提高性能
5. **前后端分离**：后端提供 API，前端灵活调用

### 技术栈

| 层级 | 技术 |
|------|------|
| 前端 | React + TypeScript |
| 后端 | Flask Blueprint |
| ASR | FunASR + SenseVoice |
| TTS | Edge-TTS (微软云) |

---

*文档生成时间: 2024年12月*
*项目: 养生之道智能健康管理系统*
